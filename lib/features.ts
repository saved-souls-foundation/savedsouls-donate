/**
 * Feature flags. Sponsor-placeholders overal zichtbaar; alleen de echte betaalknop
 * kan elders worden uitgeschakeld. Op het dashboard (inlogscherm) tonen we geen sponsor-type.
 */
export const showSponsor = true;
